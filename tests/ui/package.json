{
  "name": "notes-ui-tests",
  "version": "1.0.0",
  "description": "Playwright UI tests for Notes app",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}